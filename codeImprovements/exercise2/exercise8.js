// function discount(totalamount) {
//   var disc;
//   if (totalamount > 100) {
//     disc 
//   } else {
//     disc = totalamount * 0.05;
//   }
//   return disc;
// }

let actualDiscount;
const discount = (totalAmount) => {
  return (totalAmount > 100) ? 
  actualDiscount = totalAmount * 0.1 : 
  actualDiscount = totalAmount * 0.1
}
